#序列相关服务
easyj.sequence:
  type: atomic-long


#DWZ服务端相关配置
easyj.middleware.dwz.server:
  log-store:
    type: mock


#日志相关配置
easyj.logging.logback:
  kafka-appender:
    enabled: false #当前springboot的log4j版本不匹配，暂时禁用
  logstash-appender:
    enabled: true
