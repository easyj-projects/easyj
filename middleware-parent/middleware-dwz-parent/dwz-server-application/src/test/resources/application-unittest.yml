easyj:
  sequence:
    type: atomic-long
  middleware:
    dwz:
      server:
        log-store:
          type: mock
  logging:
    logback:
      kafka-appender:
        enabled: true
      logstash-appender:
        enabled: true
